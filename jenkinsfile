pipeline {
    agent any
 
    stages {
        stage('Deploy to k8s') {
            steps {
                withKubeCredentials(kubectlCredentials: [[caCertificate: '', clusterName: 'EKS-1', contextName: '', credentialsId: 'k8s-cred', namespace: 'webapps', serverUrl: 'https://092AF84960BCE1AC42DDC81BCCA0296A.gr7.ap-south-1.eks.amazonaws.com']]) {
                    sh 'kubectl apply -f deployment-service.yml'

                       }
            }
        }
        
        stage('verification') {
            steps {
                withKubeCredentials(kubectlCredentials: [[caCertificate: '', clusterName: 'EKS-1', contextName: '', credentialsId: 'k8s-cred', namespace: 'webapps', serverUrl: 'https://092AF84960BCE1AC42DDC81BCCA0296A.gr7.ap-south-1.eks.amazonaws.com']]) {
                    sh 'kubectl get all -n webapps'

                 }
            }
        }
    }
}
